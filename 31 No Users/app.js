"use strict";
let users = ['dua', 'farwa', 'hassan', 'abbas'];
if (users.length === 0) {
    console.log("We need to find some uers");
}
else {
    users = [];
    console.log("All users have been removed " + users.length);
}
